openapi: 3.0.0
Info:
  title: Sample API
  description: Optional multi line or single-line description.
servers:
  - url: http://api.example.com/v1
    description: Optional server description
paths:
  /users:
    get:
      summary: Returns a list of users
      description: Optional extended description
      responses:
        ‘200’:
	        description: A JSON array of user names
	        content:
	          application/json:
	            schema:
	              type: array
	              items:
	               type: string